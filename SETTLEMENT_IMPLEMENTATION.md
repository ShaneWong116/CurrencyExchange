# 结余功能实现文档

## 📋 概述

本文档说明了货币交换系统的结余功能实现详情，包括API接口、后台管理界面和优化内容。

---

## 🎯 功能实现清单

### ✅ API 接口（已完成）

#### 1. 结余预览接口
**接口**: `GET /api/settlements/preview`

**功能**：获取当前结余预览数据

**返回数据**：
```json
{
  "success": true,
  "data": {
    // 当前状态
    "current_capital": 1000000,           // 当前本金
    "current_hkd_balance": 526315,        // 当前港币结余
    "rmb_balance_total": 500000,          // 人民币余额总计
    
    // 计算结果
    "settlement_rate": 0.976,             // 结余汇率（3位小数）
    "profit": 400,                        // 利润
    
    // 预期结余后状态
    "expected_new_capital": 1000400,      // 预期新本金（不含其他支出）
    "expected_new_hkd_balance": 526715,   // 预期新港币结余
    
    // 未结余交易统计
    "unsettled_income_count": 2,          // 未结余入账交易数
    "unsettled_outcome_count": 1,         // 未结余出账交易数
    "unsettled_income_rmb": 28500,        // 未结余入账人民币总额
    "unsettled_income_hkd": 30000,        // 未结余入账港币总额
    "unsettled_outcome_rmb": 14250,       // 未结余出账人民币总额
    "unsettled_outcome_hkd": 15000,       // 未结余出账港币总额
    "outcome_hkd_cost": 14600,            // 出账港币成本
    
    // 是否可以执行结余
    "can_settle": true
  }
}
```

---

#### 2. 执行结余接口
**接口**: `POST /api/settlements`

**请求体**：
```json
{
  "expenses": [
    {
      "item_name": "薪金",
      "amount": 100
    },
    {
      "item_name": "金流费用",
      "amount": 200
    }
  ],
  "notes": "10月份结余"
}
```

**返回数据**：
```json
{
  "success": true,
  "message": "结余操作成功",
  "data": {
    "settlement": {
      "id": 1,
      "sequence_number": 1,
      "previous_capital": 1000000,
      "previous_hkd_balance": 526315,
      "profit": 400,
      "other_expenses_total": 300,
      "new_capital": 1000100,
      "new_hkd_balance": 526715,
      "settlement_rate": 0.976,
      "rmb_balance_total": 514250,
      "notes": "10月份结余",
      "created_at": "2025-10-23 18:00:00"
    },
    "expenses": [
      {
        "id": 1,
        "item_name": "薪金",
        "amount": 100
      },
      {
        "id": 2,
        "item_name": "金流费用",
        "amount": 200
      }
    ],
    "transactions_count": 3,
    "income_transactions_count": 2,
    "outcome_transactions_count": 1
  }
}
```

---

#### 3. 获取结余详情接口
**接口**: `GET /api/settlements/{id}`

**返回数据**：同"执行结余接口"返回的data字段

---

#### 4. 获取结余历史列表接口
**接口**: `GET /api/settlements?page=1&per_page=20`

**返回数据**：
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "sequence_number": 1,
      "previous_capital": 1000000,
      "profit": 400,
      "new_capital": 1000100,
      "settlement_rate": 0.976,
      "created_at": "2025-10-23 18:00:00",
      "expenses": [...]
    }
  ],
  "pagination": {
    "total": 10,
    "per_page": 20,
    "current_page": 1,
    "last_page": 1,
    "from": 1,
    "to": 10
  }
}
```

---

### ✅ 后台管理界面（已完成）

#### 1. 结余管理资源
**路径**: `/admin/settlements`

**功能**：
- ✅ 查看结余历史列表
- ✅ 按时间范围筛选
- ✅ 查看单条结余详情
- ✅ 执行新的结余操作（通过Header Action）

**列表展示字段**：
- 序号
- 结余前本金
- 利润（带颜色标识正负）
- 其他支出
- 结余后本金
- 结余汇率
- 人民币余额
- 关联交易数
- 结余时间

---

#### 2. 执行结余操作（Modal 模态框）

**触发位置**: 结余列表页面右上角"执行结余"按钮

**功能**：
1. **结余预览区**：
   - 当前本金、港币结余、人民币余额
   - 结余汇率、利润
   - 未结余交易统计
   - 预期结余后状态

2. **其他支出区**：
   - 动态添加/删除支出项目
   - 每项包含：支出项目名称、金额

3. **备注区**：
   - 可选填写备注信息

4. **业务校验**：
   - 如果没有未结余交易，禁用按钮并提示
   - 如果结余汇率异常，执行时报错

**提交后**：
- 显示成功通知
- 自动跳转到新创建的结余详情页

---

#### 3. 结余详情页

**路径**: `/admin/settlements/{id}`

**展示内容**：
1. **结余概览**：序号、时间、汇率
2. **本金变化**：结余前本金、利润、其他支出、结余后本金
3. **港币结余变化**：结余前后对比、人民币余额
4. **其他支出明细**：列表展示所有支出项目
5. **关联交易统计**：总数、入账数、出账数
6. **备注**（如有）

---

#### 4. Dashboard Widget

**Widget**: `SettlementStatsWidget`

**位置**: 后台首页

**展示内容**：
- 当前结余汇率
- 预计利润（基于未结余交易）
- 未结余交易数量
- 上次结余时间

---

## 🔧 优化内容

### 1. 精度优化
✅ **结余汇率精度**：从5位小数调整为3位小数
```php
// 修改前
$settlementRate = round($rmbTotal / $hkdTotal, 5);

// 修改后
$settlementRate = round($rmbTotal / $hkdTotal, 3);
```

### 2. 业务校验增强
✅ **添加业务规则校验**：
- 没有未结余交易时禁止执行结余
- 结余汇率为0或负数时禁止执行

```php
if (!$preview['can_settle']) {
    throw new Exception('当前没有未结余的交易，无法执行结余操作');
}

if ($preview['settlement_rate'] <= 0) {
    throw new Exception('结余汇率计算异常，请检查港币结余是否正确');
}
```

### 3. 预览数据增强
✅ **新增字段**：
- `expected_new_capital`: 预期新本金（方便前端展示）
- `expected_new_hkd_balance`: 预期新港币结余
- `can_settle`: 是否可以执行结余

### 4. 代码优化
✅ **Service层优化**：
- 支出明细为空时不创建记录
- 金额四舍五入保证精度
- 事务处理保证数据一致性

✅ **Controller层优化**：
- 完善的数据验证
- 友好的错误提示
- 中文错误消息

---

## 📊 数据库结构

### settlements 表
```sql
CREATE TABLE settlements (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    sequence_number INT NOT NULL,              -- 结余序号
    previous_capital DECIMAL(15,2) NOT NULL,   -- 结余前本金
    previous_hkd_balance DECIMAL(15,2) NOT NULL, -- 结余前港币结余
    profit DECIMAL(15,3) NOT NULL,             -- 利润
    other_expenses_total DECIMAL(15,2) NOT NULL, -- 其他支出总额
    new_capital DECIMAL(15,2) NOT NULL,        -- 结余后本金
    new_hkd_balance DECIMAL(15,2) NOT NULL,    -- 结余后港币结余
    settlement_rate DECIMAL(10,5) NOT NULL,    -- 结余汇率
    rmb_balance_total DECIMAL(15,2) NOT NULL,  -- 人民币余额总计
    notes TEXT,                                -- 备注
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### settlement_expenses 表
```sql
CREATE TABLE settlement_expenses (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    settlement_id BIGINT NOT NULL,             -- 所属结余ID
    item_name VARCHAR(100) NOT NULL,           -- 支出项目名称
    amount DECIMAL(15,2) NOT NULL,             -- 金额
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    FOREIGN KEY (settlement_id) REFERENCES settlements(id)
);
```

---

## 🔐 权限控制

### 资源级权限
- ❌ **不允许创建**：通过自定义Action执行结余
- ❌ **不允许编辑**：结余记录不可修改
- ❌ **不允许删除**：保护历史数据
- ✅ **允许查看**：查看结余详情和列表

---

## 🚀 使用流程

### 前台用户（通过API）
1. 调用预览接口查看当前状态
2. 准备其他支出数据
3. 调用执行接口完成结余
4. 查看结余详情或历史

### 后台管理员（通过Filament）
1. 登录后台 `/admin`
2. 点击导航栏"结余管理"
3. 查看Dashboard上的结余统计Widget
4. 点击列表页"执行结余"按钮
5. 在弹窗中查看预览数据
6. 添加其他支出项目（可选）
7. 填写备注（可选）
8. 点击"确认执行结余"
9. 自动跳转到详情页查看结果

---

## 📝 计算公式总结

### 1. 当前结余汇率
```
人民币总量 = 各渠道人民币余额汇总 + 未结余入账人民币金额之和
港币总量 = 当前港币结余 + 未结余入账港币金额之和
当前结余汇率 = 人民币总量 ÷ 港币总量（保留3位小数）
```

### 2. 利润
```
出账港币总额 = SUM(未结余出账交易的港币金额)
出账港币成本 = SUM(未结余出账交易的人民币金额) ÷ 当前结余汇率
利润 = 出账港币总额 - 出账港币成本（保留3位小数）
```

### 3. 结余后更新
```
结余后本金 = 当前本金 + 利润 - 其他支出总额
结余后港币结余 = 当前港币结余 + 利润
```

---

## ⚠️ 注意事项

1. **数据一致性**：结余操作使用数据库事务，确保原子性
2. **精度控制**：所有金额计算保留2位小数，汇率保留3位小数
3. **历史保护**：结余记录不可编辑和删除，保护历史数据
4. **状态更新**：结余后自动更新所有未结余交易的状态为"已结余"
5. **系统设置**：结余后自动更新系统本金和港币结余设置

---

## 🎨 界面预览

### 结余列表页
```
┌────────────────────────────────────────────────────────┐
│ 结余管理                          [执行结余] 按钮      │
├────────────────────────────────────────────────────────┤
│ 序号 | 结余前本金 | 利润 | 支出 | 结余后本金 | 时间  │
│  3   | 1,000,100 | +500 | 200  | 1,000,400 | 10-23  │
│  2   | 1,000,000 | +400 | 300  | 1,000,100 | 10-22  │
│  1   | 1,000,000 | -200 | 100  | 1,000,000 | 10-21  │
└────────────────────────────────────────────────────────┘
```

### 执行结余弹窗
```
┌───────────────────── 执行结余操作 ─────────────────────┐
│                                                        │
│ 【结余预览】                                           │
│ 当前本金: 1,000,000 HKD                               │
│ 当前港币结余: 526,315 HKD                              │
│ 人民币余额: 500,000 CNY                               │
│                                                        │
│ 【计算结果】                                           │
│ 结余汇率: 0.976                                       │
│ 利润: +400.00 HKD (绿色)                              │
│ 未结余交易: 入账 2 笔 | 出账 1 笔                      │
│                                                        │
│ 【预期结余后状态】                                     │
│ 预期新本金: 1,000,400 HKD (大字体、绿色)              │
│ 预期新港币结余: 526,715 HKD (大字体、绿色)            │
│                                                        │
│ 【其他支出】                                           │
│ ┌─ 支出项目: [薪金____] 金额: [100] HKD [删除]       │
│ ┌─ 支出项目: [金流费用] 金额: [200] HKD [删除]       │
│ [+ 添加支出项目]                                      │
│                                                        │
│ 【备注】                                               │
│ ┌──────────────────────────────────────┐            │
│ │ 10月份结余                            │            │
│ └──────────────────────────────────────┘            │
│                                                        │
│             [取消]  [确认执行结余]                    │
└────────────────────────────────────────────────────────┘
```

---

**文档版本**: v1.0  
**创建日期**: 2025-10-23  
**最后更新**: 2025-10-23
